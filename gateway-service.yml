spring:
  cloud:
    gateway:
      routes:
      - id: learning-center-service
        uri: lb://learning-center-service
        path: /api/**
      - id: paymentService
        uri: lb://PAYMENT-SERVICE
        path: /payment/**
