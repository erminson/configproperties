spring:
  cloud:
    gateway:
      routes:
      - id: learningCenter
        uri: lb://learning-center-service
        predicates:
        - Path=/api/**
      - id: paymentService
        uri: lb://payment-service
        predicates:
        - Path=/payment/**
